import React from 'react';

const viewports = ['tiny', 'small', 'medium', 'large', 'huge', 'enormous'];
const selector = '#root';

export { viewports, selector };

export default () => (
  <div>Testing that wdio test dev site snapshots works.</div>
);
