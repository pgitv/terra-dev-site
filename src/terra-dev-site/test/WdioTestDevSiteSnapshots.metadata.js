const viewports = ['tiny', 'small', 'medium', 'large', 'huge', 'enormous'];
const selector = '#root';
const themeableProperties = {
  '--wdio-test-dev-site-snapshots-text-color': 'purple',
};

export default {
  viewports,
  selector,
  themeableProperties,
};
